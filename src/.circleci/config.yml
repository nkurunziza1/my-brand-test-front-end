jobs:
  build:
    docker:
      - image: node:8.19.3
    steps:
      - checkout:
          
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test --coverage -- --verbose